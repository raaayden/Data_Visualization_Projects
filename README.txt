Install

Install requirements
    - pip install -r requirements.txt 
    or
    - pip install flask

Access

set FLASK_ENV=development
set FLASK_APP=app.py
flask run

1. Open command prompt
2. pip install flask
3. Directory to the folder 
4. python app.py